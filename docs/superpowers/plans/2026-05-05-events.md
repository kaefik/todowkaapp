# События (Calendar Events) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить поддержку событий с полями location/attendees, привязкой задач к событиям, и отдельной страницей /events в сайдбаре.

**Architecture:** Расширение модели CalendarEvent новыми полями, добавление ForeignKey в Task, создание отдельного роута /events, интеграция с Calendar и TaskEditModal.

**Tech Stack:** Python 3.12+, FastAPI, SQLAlchemy 2.0 (async), React 18+, TypeScript, Dexie, Zustand

---

## Task 1: Расширение модели CalendarEvent (backend)

**Files:**
- Modify: `backend/app/models/calendar_event.py`
- Modify: `backend/app/schemas/calendar_event.py`
- Test: `backend/tests/test_calendar_events.py` (создать если нет)

- [ ] **Step 1: Добавить поля location и attendees в модель CalendarEvent**

```python
# backend/app/models/calendar_event.py
# Добавить после поля color:
location: Mapped[str | None] = mapped_column(String(500), nullable=True)
attendees: Mapped[list | None] = mapped_column(JSON, nullable=True)
```

- [ ] **Step 2: Обновить схемы Pydantic**

```python
# backend/app/schemas/calendar_event.py
# CalendarEventCreate добавить:
location: str | None = Field(default=None, max_length=500)
attendees: list[str] | None = Field(default=None)

# CalendarEventUpdate добавить:
location: str | None = Field(default=None, max_length=500)
attendees: list[str] | None = Field(default=None)

# CalendarEventResponse добавить:
location: str | None
attendees: list[str] | None
```

- [ ] **Step 3: Обновить service create_event**

```python
# backend/app/services/calendar_event_service.py
# В методе create_event добавить:
event.location = data.location
event.attendees = data.attendees
```

- [ ] **Step 4: Создать миграцию Alembic**

```bash
cd backend
alembic revision --autogenerate -m "add location and attendees to calendar_events"
alembic upgrade head
```

- [ ] **Step 5: Запустить тест**

Run: `pytest backend/tests/test_calendar_events.py -v`

---

## Task 2: Добавить event_id в Task (backend)

**Files:**
- Modify: `backend/app/models/task.py`
- Modify: `backend/app/schemas/task.py`
- Modify: `backend/app/services/task_service.py`
- Test: `backend/tests/test_tasks.py`

- [ ] **Step 1: Добавить поле event_id в модель Task**

```python
# backend/app/models/task.py
# Добавить после field project_id:
event_id: Mapped[str | None] = mapped_column(
    String(36), ForeignKey('calendar_events.id', ondelete='SET NULL'), nullable=True
)

# Добавить relationship:
event = relationship('CalendarEvent', back_populates='tasks')
```

- [ ] **Step 2: Обновить CalendarEvent model с back_populates**

```python
# backend/app/models/calendar_event.py
# Добавить relationship:
tasks = relationship('Task', back_populates='event')
```

- [ ] **Step 3: Обновить Pydantic схемы Task**

```python
# backend/app/schemas/task.py
# В TaskCreate и TaskUpdate добавить:
event_id: str | None = None

# В TaskResponse добавить:
event_id: str | None
```

- [ ] **Step 4: Обновить TaskService create/update**

```python
# backend/app/services/task_service.py
# В create_task и update_task добавить обработку event_id
```

- [ ] **Step 5: Создать миграцию Alembic**

```bash
cd backend
alembic revision --autogenerate -m "add event_id to tasks"
alembic upgrade head
```

- [ ] **Step 6: Запустить тест**

Run: `pytest backend/tests/test_tasks.py -v`

---

## Task 3: API эндпоинт для списка событий (backend)

**Files:**
- Modify: `backend/app/api/calendar_events.py`

- [ ] **Step 1: Добавить эндпоинт /events-for-select**

```python
# backend/app/api/calendar_events.py
# Добавить после list_events:
@calendar_events_router.get('/for-select', response_model=list[CalendarEventSelectItem])
async def get_events_for_select(
    request: Request,
    current_user: Annotated[User, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> list[CalendarEventSelectItem]:
    service = CalendarEventService(db)
    events = await service.get_events_for_select(user_id=current_user.id)
    return events

# где CalendarEventSelectItem:
class CalendarEventSelectItem(BaseModel):
    id: str
    title: str
    start_time: datetime

    model_config = {'from_attributes': True}
```

- [ ] **Step 2: Добавить метод в service**

```python
# backend/app/services/calendar_event_service.py
async def get_events_for_select(self, user_id: UUID) -> list[CalendarEvent]:
    result = await self.db.execute(
        select(CalendarEvent)
        .where(CalendarEvent.user_id == user_id)
        .order_by(CalendarEvent.start_time.asc())
    )
    return list(result.scalars().all())
```

- [ ] **Step 3: Тест**

Run: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/calendar-events/for-select`

---

## Task 4: Frontend — обновить Dexie и API

**Files:**
- Modify: `frontend/src/db/database.ts`
- Modify: `frontend/src/db/mappers.ts`
- Create: `frontend/src/api/calendarEvents.ts`
- Modify: `frontend/src/api/index.ts`

- [ ] **Step 1: Обновить Dexie DbTask — добавить eventId**

```typescript
// frontend/src/db/database.ts
// В интерфейсе DbTask добавить:
eventId: string | null
```

- [ ] **Step 2: Обновить Dexie DbCalendarEvent**

```typescript
// frontend/src/db/database.ts
// В интерфейсе DbCalendarEvent добавить:
location: string | null
attendees: string[] | null
```

- [ ] **Step 3: Обновить мапперы**

```typescript
// frontend/src/db/mappers.ts
// Обновить mapDbTaskToTask и mapTaskToDbTask с eventId
// Обновить mapDbCalendarEventToCalendarEvent и mapCalendarEventToDbCalendarEvent с location/attendees
```

- [ ] **Step 4: Создать API клиент**

```typescript
// frontend/src/api/calendarEvents.ts
// CRUD методы: getEvents, getEvent, createEvent, updateEvent, deleteEvent, getEventsForSelect
// Типы: CalendarEvent, CalendarEventCreate, CalendarEventUpdate
```

---

## Task 5: Frontend — страница /events

**Files:**
- Modify: `frontend/src/router.tsx`
- Create: `frontend/src/routes/Events.tsx`
- Create: `frontend/src/components/EventEditModal.tsx`
- Modify: `frontend/src/stores/tasksStore.ts` (may not exist, check)

- [ ] **Step 1: Добавить роут /events**

```typescript
// frontend/src/router.tsx
# Добавить:
{ path: '/events', element: <Events /> },
```

- [ ] **Step 2: Создать страницу Events.tsx (аналог Tasks.tsx)**

- [ ] **Step 3: Создать EventEditModal**

---

## Task 6: Frontend — добавить Events в Sidebar

**Files:**
- Modify: `frontend/src/components/Sidebar.tsx`

- [ ] **Step 1: Добавить пункт «События» в Sidebar**

Вне GTD-секции, после Календарь, с и��онкой календаря и count-бейджем.

---

## Task 7: Frontend — интеграция TaskEditModal

**Files:**
- Modify: `frontend/src/components/TaskEditModal.tsx`

- [ ] **Step 1: Добавить поле выбора события**

Dropdown с событиями из /api/calendar-events/for-select.

---

## Task 8: Frontend — интеграция Calendar

**Files:**
- Modify: `frontend/src/routes/Calendar.tsx`
- Modify: `frontend/src/components/calendar/DayDetailDrawer.tsx`

- [ ] **Step 1: Показывать связанные задачи под событием**

В DayDetailDrawer при клике на событие показать список связанных задач с чекбоксами.

---

## Task 9: i18n

**Files:**
- Modify: `frontend/src/i18n/locales/ru/common.json`
- Modify: `frontend/src/i18n/locales/en/common.json`
- Create: `frontend/src/i18n/locales/ru/events.json`
- Create: `frontend/src/i18n/locales/en/events.json`

- [ ] **Step 1: Добавить переводы**

Ключи: events.title, events.create, events.edit, events.location, events.attendees, events.noEvents, events.linkedTasks и т.д.

---

## Task 10: Тестирование и фиксы

- [ ] **Step 1: E2E тест создания события с задачей**

- [ ] **Step 2: Тест привязки задачи к событию**

- [ ] **Step 3: Тест отображения в Calendar**

Run: `cd frontend && npm run lint && npx tsc --noEmit`

---

## Проверки перед коммитом

```bash
cd backend
ruff check .
pytest tests/ -v

cd frontend
npm run lint
npx tsc --noEmit
```