# План реализации Итерации 2 — GTD-методология

---

## Обзор

Документ определяет оптимальный порядок реализации всех 11 фич Итерации 2 с учётом:
- Технических зависимостей
- Сложности реализации
- Проверки функционала на каждом этапе
- Возможности параллельной работы

---

## Стратегия реализации

### Принципы

1. **Снизу-вверх** — от модели данных к UI
2. **От простого к сложному** — сначала простые CRUD-сущности
3. **Ранний feedback** — MVP каждой фичи как можно раньше
4. **Параллельность** — независимые фичи можно делать одновременно
5. **Постоянная работоспособность** — после каждого этапа система должна быть функциональной

### Этапы реализации

```
Этап 0: Подготовка
  └─> Настройка окружения, резервное копирование

Этап 1: Фундамент — Модель данных
  └─> Все новые таблицы + миграции

Этап 2: Простые сущности (параллельно)
  ├─> Контексты
  ├─> Области
  └─> Теги

Этап 3: Проекты
  └─> Сложная сущность с прогресс-баром

Этап 4: GTD-ядро
  ├─> GTD-статусы
  └─> Inbox (быстрый захват)

Этап 5: Подзадачи
  └─> Иерархия задач

Этап 6: Поиск и фильтрация
  └─> Комбинированные фильтры + поиск

Этап 7: Интеграция UI
  ├─> Форма задачи (все поля)
  ├─> Sidebar (навигация)
  └─> Роутинг (все страницы)

Этап 8: Финализация
  └─> Тестирование, рефакторинг, документация
```

---

## Детальный план

### Этап 0: Подготовка (2 часа)

**Цель:** Готовность к работе без риска сломать существующее

**Задачи:**
- [x] Создать бэкап текущей БД
- [x] Проверить, что все тесты из Итерации 1 проходят
- [x] Обновить документацию (`docs/features.md` с планом Итерации 2)
- [x] Создать ветку `feature/gtd-iteration-2`

**Критерий завершения:** Система работает, тесты зелёные, есть бэкап ✅ (Выполнено 10.04.2026)

---

### Этап 1: Модель данных (6-8 часов)

**Цель:** Фундамент для всех фич

**Задачи:**
- [x] ORM-модель `contexts`
- [x] ORM-модель `areas`
- [x] ORM-модель `tags`
- [x] Связующая таблица `task_tags` (M:N)
- [x] ORM-модель `projects`
- [x] Расширение модели `tasks` (gtd_status, context_id, area_id, project_id, parent_task_id, position, due_date, notes)
- [x] Создать индексы (8 индексов)
- [x] Alembic миграция `002_add_contexts_areas_tags`
- [x] Alembic миграция `003_add_projects`
- [x] Alembic миграция `004_extend_tasks_gtd`
- [x] Проверить миграции: up + down

**Критерий завершения:** Миграции применены, таблицы созданы, индексы есть ✅ (Выполнено 10.04.2026)

**Зависимости:** Нет (первый этап)

**Тестирование:**
- Проверить, что старые задачи работают (обратная совместимость)
- Проверить, что миграция up/down работает

---

### Этап 2: Простые сущности (12-16 часов, можно параллельно)

**Цель:** Базовые CRUD-сущности, независимые друг от друга

#### 2.1 Контексты (5-7 часов)

**Backend:**
- [x] Pydantic schemas: `ContextCreate`, `ContextUpdate`, `ContextResponse`
- [x] `ContextService` — CRUD + проверка уникальности имени
- [x] API endpoints: `GET/POST /api/contexts`, `PUT/DELETE /api/contexts/{id}`
- [x] Тесты CRUD контекстов

**Frontend:**
- [x] Хук `useContexts()` — CRUD
- [x] UI управления контекстами (список, создание, редактирование, удаление)
- [x] Dropdown выбора контекста в форме задачи

**Критерий завершения:** Можно создать/редактировать/удалить контекст, назначить задаче ✅ (Выполнено 10.04.2026)

#### 2.2 Области (4-6 часов)

**Backend:**
- [x] Pydantic schemas: `AreaCreate`, `AreaUpdate`, `AreaResponse`
- [x] `AreaService` — CRUD
- [x] API endpoints: `GET/POST /api/areas`, `PUT/DELETE /api/areas/{id}`
- [x] Тесты CRUD областей

**Frontend:**
- [x] Хук `useAreas()` — CRUD
- [x] UI управления областями
- [x] Dropdown выбора области в форме задачи

**Критерий завершения:** Можно создать/редактировать/удалить область, назначить задаче/проекту ✅ (Выполнено 10.04.2026)

#### 2.3 Теги (6-8 часов)

**Backend:**
- [x] Pydantic schemas: `TagCreate`, `TagUpdate`, `TagResponse`
- [x] `TagService` — CRUD + привязка/отвязка тегов к задачам
- [x] API endpoints: `GET/POST /api/tags`, `PUT/DELETE /api/tags/{id}`
- [x] API endpoints: `POST/DELETE /api/tasks/{id}/tags`
- [x] Тесты CRUD тегов + привязка/отвязка

**Frontend:**
- [x] Хук `useTags()` — CRUD
- [x] Компонент multi-select тегов (chips) в форме задачи
- [x] Цветные chips тегов в списке задач
- [x] UI управления тегами (CRUD)

**Критерий завершения:** Можно создать/редактировать/удалить тег, назначить несколько тегов задаче ✅ (Выполнено 11.04.2026)

**Примечание:** Этапы 2.1, 2.2, 2.3 можно делать параллельно разными разработчиками

---

### Этап 3: Проекты (8-12 часов) ✅ (Выполнено 11.04.2026)

**Цель:** Сложная сущность с прогресс-баром

**Backend:**
- [x] Pydantic schemas: `ProjectCreate`, `ProjectUpdate`, `ProjectResponse` (с progress)
- [x] `ProjectService` — CRUD + подсчёт прогресса (tasks_total, tasks_completed, progress_percent)
- [x] API endpoints: `GET/POST /api/projects`, `GET/PUT/DELETE /api/projects/{id}`
- [x] API endpoint: `GET /api/projects/{id}/tasks`
- [x] Тесты CRUD проектов + прогресс

**Frontend:**
- [x] Хук `useProjects()` — CRUD
- [x] Страница `/projects` — список проектов (карточки с прогресс-баром)
- [x] Страница `/projects/{id}` — детали проекта + список задач проекта
- [x] Форма создания/редактирования проекта (модал)
- [x] Dropdown выбора проекта в форме задачи

**Критерий завершения:** Можно создать проект, добавить задачи, видеть прогресс-бар ✅

**Зависимости:** Этап 1 (модель данных), Этап 2.2 (области для area_id)

---

### Этап 4: GTD-ядро (10-14 часов)

**Цель:** Фундамент GTD-методологии

#### 4.1 GTD-статусы (6-8 часов) ✅ (Выполнено 11.04.2026)

**Backend:**
- [x] Обновить `TaskService`: `move_task()`, `reorder_task()`, фильтрация по `gtd_status`
- [x] Обновить Pydantic schemas задач: добавить gtd_status, context_id, area_id, project_id, parent_task_id, position, due_date, notes, tag_ids
- [x] API endpoint: `PATCH /api/tasks/{id}/move` — смена gtd_status
- [x] API endpoint: `PATCH /api/tasks/{id}/reorder` — смена position
- [x] Обновить `GET /api/tasks` — поддержка query params: gtd_status, context_id, area_id, project_id, tag_id, is_completed, due_date_from, due_date_to, search, sort_by, sort_order
- [x] Тесты GTD move_task, фильтрации по статусу

**Frontend:**
- [x] Страница `/inbox` — список задач Inbox
- [x] Страница `/next` — список Next Actions
- [x] Страница `/waiting` — список Waiting For
- [x] Страница `/someday` — список Someday / Maybe
- [x] Страница `/completed` — список выполненных задач
- [x] Хук `useGtdCounts()` — счётчики задач по GTD-статусам

**Критерий завершения:** Можно создавать задачи в Inbox, перемещать между статусами ✅

#### 4.2 Inbox — быстрый захват (4-6 часов) ✅ (Выполнено 11.04.2026 — частично)

**Backend:**
- [x] Изменить `POST /api/tasks` — default `gtd_status=inbox`
- [~] ~~API endpoint: `PATCH /api/tasks/{id}/clarify` — быстрое перемещение из Inbox~~ — не реализовано, не планируется (используется комбинация `PUT /tasks/{id}` + `PATCH /tasks/{id}/move`)

**Frontend:**
- [~] ~~Quick Capture Bar — компонент быстрого добавления (input + Enter → Inbox)~~ — не реализовано, не планируется (достаточно инлайн-формы на странице Inbox)
- [~] ~~Клавиатурный шорткат фокуса на Quick Capture (Ctrl+K или N)~~ — не реализовано, не планируется
- [x] Inline-форма уточнения задачи в Inbox (назначить контекст, проект, дедлайн, перевести в статус) — реализовано через расширенный TaskEditModal (gtd_status, due_date, notes добавлены)
- [x] Кнопки быстрого перемещения в Inbox (→ Next, → Waiting, → Someday, → Trash)

**Критерий завершения:** Можно быстро добавить задачу в Inbox, затем её уточнить ✅

**Зависимости:** Этап 1, Этап 2 (контексты, проекты), Этап 4.1 (GTD-статусы)

---

### Этап 5: Подзадачи (6-8 часов) ✅ (Выполнено 11.04.2026)

**Цель:** Иерархия задач

**Backend:**
- [x] Обновить `GET /api/tasks` — по умолчанию только корневые (parent_task_id IS NULL), param `?include_subtasks=true`
- [x] API endpoint: `GET /api/tasks/{id}/subtasks` — список подзадач
- [x] API endpoint: `POST /api/tasks/{id}/subtasks` — создание подзадачи
- [x] Добавить в `TaskResponse`: `subtasks_count`, `subtasks_completed`
- [x] Тесты подзадач (создание, получение, toggle, subtasks_count, include_subtasks, cascade delete)

**Frontend:**
- [x] Хук `useSubtasks(parentTaskId)` — CRUD подзадач
- [x] UI подзадач в карточке задачи: индикатор (3/5), раскрываемый список, кнопка «+ Добавить подзадачу»
- [x] Toggle подзадачи (чекбокс is_completed)

**Критерий завершения:** Можно создать подзадачу, видеть прогресс, toggle ✅

**Зависимости:** Этап 1 (parent_task_id в модели), Этап 4 (GTD-статусы)

---

### Этап 6: Поиск и фильтрация (8-10 часов) ✅ (Выполнено 12.04.2026)

**Цель:** Мощная система поиска и фильтрации

**Backend:**
- [x] `TaskFilterService.build_query()` — логика встроена в `TaskService.get_tasks()` (без выделенного класса)
- [x] Полнотекстовый поиск по title + description + notes (LIKE %query% COLLATE NOCASE)
- [x] Сортировка: по position, created_at, due_date, title + asc/desc
- [x] Расширить `GET /api/tasks` — 13 query params
- [x] Тесты комбинированной фильтрации, поиска, сортировки, пагинации (13 новых тестов)

**Frontend:**
- [x] Панель фильтров: dropdowns (GTD-статус, контекст, область, проект, тег), date range picker дедлайна
- [x] Строка поиска с debounce 300ms + иконка + очистка
- [x] Dropdown сортировки (по позиции / дате / дедлайну / названию) + toggle asc/desc
- [x] Сохранение фильтров в URL query params
- [x] Подсветка найденного текста в результатах поиска (highlight)

**Критерий завершения:** Можно комбинировать любые фильтры, искать, сортировать, делиться ссылками ✅

**Зависимости:** Все предыдущие этапы (нужны все сущности для фильтров)

---

### Этап 7: Интеграция UI (8-10 часов)

**Цель:** Единый пользовательский интерфейс

#### 7.1 Sidebar (3-4 часа) ✅ (Выполнено 12.04.2026 — частично)

**Frontend:**
- [x] Компонент `Sidebar` — структура: GTD-секции, Проекты, Контексты, Области, Теги
- [x] Sidebar badges — счётчики задач у каждого GTD-статуса (запрос `/api/tasks/counts`)
- [~] ~~Sidebar мини-прогрессбар у проектов~~ — не реализовано, не планируется (данные доступны через `useProjects`, но UI не добавлялся; достаточно прогрессбара на странице проектов)
- [x] Mobile sidebar: скрыт, slide-over через бургер-меню
- [~] ~~Desktop sidebar: collapsible (свернуть в иконки)~~ — не реализовано, не планируется (фиксированная ширина достаточна; collapsible — UI-украшение, не критично)

**Критерий завершения:** Sidebar навигирует по всем разделам, счётчики обновляются ✅

#### 7.2 Форма задачи (3-4 часа)

**Frontend:**
- [ ] Обновить форму задачи: добавить поля gtd_status, контекст, проект, область, дедлайн, теги, заметки
- [ ] Zod schema валидации формы с новыми полями
- [ ] Автозаполнение контекста/проекта при создании из контекста sidebar
- [ ] Progressive disclosure (базовые поля видны, остальные в «Дополнительно»)

**Критерий завершения:** Форма позволяет создать задачу со всеми параметрами

#### 7.3 Роутинг (1-2 часа)

**Frontend:**
- [ ] Обновить роутер: добавить маршруты /inbox, /next, /waiting, /someday, /completed, /projects, /projects/:id, /contexts, /areas, /tags, /tasks/:id
- [ ] Проверить deep linking

**Критерий завершения:** Все страницы доступны по URL, можно делиться ссылками

**Зависимости:** Все предыдущие этапы

---

### Этап 8: Финализация (10-14 часов)

**Цель:** Качество, тестирование, документация

**Задачи:**

**Backend тесты:**
- [ ] Тесты CRUD контекстов (успех, уникальность, удаление с обнулением FK)
- [ ] Тесты CRUD областей
- [ ] Тесты CRUD тегов + привязка/отвязка тегов к задаче
- [ ] Тесты CRUD проектов + прогресс (tasks_total, tasks_completed, progress_percent)
- [ ] Тесты подзадач (создание, получение, toggle, subtasks_count)
- [ ] Тесты GTD move_task (смена статуса)
- [ ] Тесты комбинированной фильтрации (несколько фильтров одновременно)
- [ ] Тесты поиска (search query, case-insensitive)
- [ ] Тесты сортировки (position, created_at, due_date, title + asc/desc)
- [ ] Тесты пагинации с фильтрами

**Frontend тесты:**
- [ ] Тест sidebar: рендеринг секций, счётчики, навигация
- [ ] Тест формы задачи: все новые поля, валидация
- [ ] Тест фильтрации + поиска
- [ ] Тест страницы проекта: прогресс-бар, список задач
- [ ] Тест подзадач: добавление, toggle, отображение count

**Качество кода:**
- [ ] Линтер: `ruff check .` (backend), `npm run lint` (frontend)
- [ ] Type-check: `mypy` / `tsc --noEmit`
- [ ] Форматирование: `ruff check --fix .`

**Документация:**
- [ ] Обновить `docs/features.md` — добавить все новые фичи
- [ ] Обновить README.md — инструкции для пользователей
- [ ] Проверить OpenAPI документацию (FastAPI auto-docs)

**UX проверка:**
- [ ] Проверить UI на mobile (320px+)
- [ ] Проверить UI на desktop
- [ ] Проверить, что все фильтры работают в комбинации
- [ ] Проверить, что URL state сохраняется

**Критерий завершения:** Все тесты проходят, документация обновлена, CI зелёный

---

## Сводная таблица этапов

| Этап | Название | Фичи | Время | Зависимости |
|------|----------|-------|-------|-------------|
| 0 | Подготовка | - | 2 ч | - |
| 1 | Модель данных | - | 6-8 ч | - |
| 2.1 | Контексты | Контексты | 5-7 ч | Этап 1 |
| 2.2 | Области | Области | 4-6 ч | Этап 1 |
| 2.3 | Теги | Теги | 6-8 ч | Этап 1 |
| 3 | Проекты | Проекты | 8-12 ч | Этап 1, 2.2 |
| 4.1 | GTD-статусы | GTD-статусы | 6-8 ч | Этап 1, 2 |
| 4.2 | Inbox | Inbox | 4-6 ч | Этап 1, 2, 4.1 |
| 5 | Подзадачи | Подзадачи | 6-8 ч | Этап 1, 4 |
| 6 | Поиск и фильтрация | Фильтрация/поиск | 8-10 ч | Все предыдущие |
| 7.1 | Sidebar | Sidebar | 3-4 ч | Все предыдущие |
| 7.2 | Форма задачи | Форма задачи | 3-4 ч | Все предыдущие |
| 7.3 | Роутинг | Роутинг | 1-2 ч | Все предыдущие |
| 8 | Финализация | Тесты, документация | 10-14 ч | Все предыдущие |
| **Итого** | | | **73-105 часов** | |

---

## Оптимизация для параллельной работы

### Параллельные потоки

**Поток A (Backend-focused):**
- Этап 1 (Модель данных)
- Этап 2.1, 2.2, 2.3 (Backend части)
- Этап 3 (Backend)
- Этап 4.1 (Backend)
- Этап 4.2 (Backend)
- Этап 5 (Backend)
- Этап 6 (Backend)

**Поток B (Frontend-focused):**
- Этап 2.1, 2.2, 2.3 (Frontend части) — после завершения Backend частей
- Этап 3 (Frontend) — после Backend части
- Этап 4.1 (Frontend) — после Backend части
- Этап 4.2 (Frontend) — после Backend части
- Этап 5 (Frontend) — после Backend части
- Этап 6 (Frontend) — после Backend части
- Этап 7 (Sidebar, Форма, Роутинг)

### Рекомендации для команды

- **1 разработчик:** Последовательная реализация по этапам
- **2 разработчика:** Один Backend, один Frontend (параллельно после Этапа 1)
- **3+ разработчиков:** Этапы 2.1, 2.2, 2.3 параллельно разными людьми

---

## Milestone-ы (контрольные точки)

### Milestone 1: Фундамент (после Этапа 1)
**Дата:** День 1
**Проверка:** Миграции применены, БД готова

### Milestone 2: Базовые сущности (после Этапа 2)
**Дата:** День 3-4
**Проверка:** Можно создавать/редактировать контексты, области, теги

### Milestone 3: Проекты (после Этапа 3)
**Дата:** День 5-6
**Проверка:** Можно создавать проекты, видеть прогресс-бар

### Milestone 4: GTD-ядро (после Этапа 4)
**Дата:** День 7-8
**Проверка:** Можно добавлять задачи в Inbox, перемещать между статусами

### Milestone 5: Подзадачи (после Этапа 5)
**Дата:** День 9
**Проверка:** Можно создавать подзадачи, видеть прогресс

### Milestone 6: Поиск и фильтрация (после Этапа 6)
**Дата:** День 11-12
**Проверка:** Можно фильтровать, искать, сортировать

### Milestone 7: Полный UI (после Этапа 7)
**Дата:** День 13-14
**Проверка:** Sidebar работает, форма полная, роутинг работает

### Milestone 8: Релиз (после Этапа 8)
**Дата:** День 16-19
**Проверка:** Все тесты проходят, документация обновлена, CI зелёный

---

## Риски и митигации

| Риск | Вероятность | Импакт | Митигация |
|------|-------------|--------|-----------|
| Задержка на Этапе 1 (сложные миграции) | Средняя | Высокий | Тестировать миграции на копии БД, все поля nullable/defaults |
| Проблемы с N+1 запросами (подзадачи, теги) | Средняя | Средний | Eager loading через SQLAlchemy, покрыть тестами |
| Перегрузка формы задачи | Низкая | Средний | Progressive disclosure, UX-тестирование |
| Сложность комбинированных фильтров | Средняя | Средний | TaskFilterService покрыт тестами на все комбинации |
| Медленный поиск с LIKE | Низкая | Низкий | FTS5 как оптимизация (отдельная задача позже) |
| Усталость команды (19 дней) | Средняя | Средний | Параллельная работа, milestone-ы для мотивации |

---

## Definition of Done для Итерации 2

- [ ] Все 95 задач из исходного плана выполнены
- [ ] Все фичи из анализа реализованы
- [ ] Все тесты проходят (backend + frontend)
- [ ] Линтер и type-check проходят
- [ ] Документация обновлена (`docs/features.md`, README.md)
- [ ] CI pipeline зелёный
- [ ] UI проверен на mobile и desktop
- [ ] Миграции проверены (up + down)
- [ ] Обратная совместимость: существующие данные не ломаются
- [ ] Пользовательская документация написана

---

## Заключение

**Общая оценка времени:** 73-105 часов (9-13 рабочих дней для 1 разработчика)

**Рекомендуемая стратегия:**
1. Последовательная реализация по этапам
2. После Этапа 1 — можно параллелить (Backend/Frontend)
3. Регулярные milestone-ы для контроля
4. Тестирование параллельно с разработкой

**Ключ к успеху:**
- Не прыгать между этапами
- Тестировать каждую фичу сразу после реализации
- Следить за производительностью (индексы, eager loading)
- Держать UI простым (progressive disclosure)
