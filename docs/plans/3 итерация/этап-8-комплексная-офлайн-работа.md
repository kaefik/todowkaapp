# Этап 8 — Комплексная офлайн работа (Вариант 6: Комбинированный подход)

## Цель
Полноценная офлайн работа приложения с кэшированием данных, офлайн очередью мутаций, автоматической синхронизацией и сервис-воркером для NetworkFirst стратегии.

## Обзор архитектуры

```
Компонент (useQuery)
    ↓
React Query (cache + retry + refetchOnReconnect)
    ↓
httpClient (error handling + offline queue)
    ↓
Service Worker (NetworkFirst: Network → Cache → IndexedDB)
    ↓
IndexedDB (data cache + mutation queue)
```

## Компоненты решения

### 1. React Query (TanStack Query)
- Автоматическое кэширование GET запросов
- Refetch при восстановлении сети
- Оптимистичные обновления
- Персистентность кэша через IndexedDB

### 2. IndexedDB
- **Хранилище `data-cache`**: кэш GET запросов (tasks, contexts, areas, projects, tags, notifications)
- **Хранилище `mutations`**: очередь мутаций (уже реализовано в Этапе 7)

### 3. Service Worker с Workbox
- **NetworkFirst стратегия** для API запросов
- **CacheFirst стратегия** для статических ресурсов
- Автоматическая очистка устаревшего кэша

### 4. httpClient с улучшенной обработкой ошибок
- Перехват 502, 503, 504 ошибок
- Автоматический retry с exponential backoff
- Сохранение мутаций в офлайн очередь (уже реализовано в Этапе 7)

---

## Детальный план реализации

### Шаг 1: Установка и настройка React Query (2-3 часа)

#### Задачи:
1. **Установка зависимостей**
   ```bash
   npm install @tanstack/react-query @tanstack/react-query-devtools
   npm install @tanstack/query-async-storage-persister @tanstack/query-sync-storage-persister
   ```

2. **Создание QueryClient конфигурации**
   - Файл: `frontend/src/lib/queryClient.ts`
   - Настройка кэша: staleTime 5 мин, cacheTime 24 часа
   - Настройка retry: 3 попытки с exponential backoff
   - Настройка refetch: refetchOnReconnect: true, refetchOnWindowFocus: false
   - Персистентность через IndexedDB

3. **Интеграция QueryClientProvider**
   - Файл: `frontend/src/main.tsx`
   - Обертка приложения QueryClientProvider
   - Подключение ReactQueryDevtools

4. **Миграция useTasks на React Query**
   - Файл: `frontend/src/hooks/useTasks.ts`
   - Замена useState/useEffect на useQuery
   - Добавление mutations для addTask, updateTask, deleteTask
   - Инвалидация кэша при мутациях

#### Реализация:

**frontend/src/lib/queryClient.ts**
```typescript
import { QueryClient } from '@tanstack/react-query'
import { createAsyncStoragePersister } from '@tanstack/query-async-storage-persister'
import { persistQueryClient } from '@tanstack/query-sync-storage-persister'

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5, // 5 минут
      gcTime: 1000 * 60 * 60 * 24, // 24 часа (раньше cacheTime)
      retry: 3,
      retryDelay: attemptIndex => Math.min(1000 * 2 ** attemptIndex, 30000),
      refetchOnWindowFocus: false,
      refetchOnReconnect: true,
      refetchOnMount: false,
    },
    mutations: {
      retry: 1,
      retryDelay: 1000,
    },
  },
})

// Персистентность через IndexedDB
const asyncStoragePersister = createAsyncStoragePersister({
  storage: {
    getItem: async (key) => {
      const db = await openDB('todowka-query-cache', 1)
      return db.get('cache', key)
    },
    setItem: async (key, value) => {
      const db = await openDB('todowka-query-cache', 1)
      await db.put('cache', value, key)
    },
    removeItem: async (key) => {
      const db = await openDB('todowka-query-cache', 1)
      await db.delete('cache', key)
    },
  },
})

persistQueryClient({
  queryClient,
  persister: asyncStoragePersister,
  maxAge: 1000 * 60 * 60 * 24 * 7, // 7 дней
})
```

**frontend/src/main.tsx**
```typescript
import { QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'
import { queryClient } from './lib/queryClient'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <AuthInitializer>
        <NotificationProvider>
          <AppRouter />
          <ToastContainer />
        </NotificationProvider>
      </AuthInitializer>
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  </StrictMode>,
)
```

**frontend/src/hooks/useTasks.ts**
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { httpClient } from '../api/httpClient'
import type { Task, CreateTask, UpdateTask } from './useTasks'

// Query keys
export const taskKeys = {
  all: ['tasks'] as const,
  lists: () => [...taskKeys.all, 'list'] as const,
  list: (filters: TaskFilters) => [...taskKeys.lists(), filters] as const,
  details: () => [...taskKeys.all, 'detail'] as const,
  detail: (id: string) => [...taskKeys.details(), id] as const,
}

// Queries
export function useTasks(filters?: TaskFilters) {
  return useQuery({
    queryKey: taskKeys.list(filters || {}),
    queryFn: async () => {
      const response = await httpClient.get<{ items: ApiTask[]; total: number }>(`/tasks${buildQueryString(filters)}`)
      return response.data.items.map(mapTask)
    },
    staleTime: 1000 * 60 * 2, // 2 минуты
  })
}

export function useTask(id: string) {
  return useQuery({
    queryKey: taskKeys.detail(id),
    queryFn: async () => {
      const response = await httpClient.get<ApiTask>(`/tasks/${id}`)
      return mapTask(response.data)
    },
    enabled: !!id,
  })
}

// Mutations
export function useAddTask() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (data: CreateTask) => {
      const response = await httpClient.post<ApiTask>('/tasks', data)
      return mapTask(response.data)
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: taskKeys.lists() })
    },
  })
}

export function useUpdateTask() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: UpdateTask }) => {
      const response = await httpClient.put<ApiTask>(`/tasks/${id}`, data)
      return mapTask(response.data)
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: taskKeys.detail(variables.id) })
      queryClient.invalidateQueries({ queryKey: taskKeys.lists() })
    },
  })
}

export function useDeleteTask() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (id: string) => {
      await httpClient.delete(`/tasks/${id}`)
      return id
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: taskKeys.lists() })
    },
  })
}
```

#### Файлы для создания/изменения:
- `frontend/src/lib/queryClient.ts` — новый файл
- `frontend/src/main.tsx` — интеграция QueryClientProvider
- `frontend/src/hooks/useTasks.ts` — миграция на React Query

---

### Шаг 2: Миграция остальных хуков на React Query (3-4 часа)

#### Задачи:
1. **Миграция useContexts**
   - Файл: `frontend/src/hooks/useContexts.ts`
   - useQuery для списка контекстов
   - useMutation для create/update/delete
   - Query keys: contextKeys

2. **Миграция useAreas**
   - Файл: `frontend/src/hooks/useAreas.ts`
   - useQuery для списка областей
   - useMutation для create/update/delete
   - Query keys: areaKeys

3. **Миграция useProjects**
   - Файл: `frontend/src/hooks/useProjects.ts`
   - useQuery для списка проектов
   - useQuery для деталей проекта
   - useMutation для create/update/delete/archive
   - Query keys: projectKeys

4. **Миграция useTags**
   - Файл: `frontend/src/hooks/useTags.ts`
   - useQuery для списка тегов
   - useMutation для create/update/delete
   - Query keys: tagKeys

5. **Миграция useNotifications**
   - Файл: `frontend/src/hooks/useNotifications.ts` (или использовать notificationStore)
   - useQuery для списка уведомлений
   - useMutation для markAsRead, markAllAsRead, delete
   - Query keys: notificationKeys

#### Реализация (пример для useContexts):

**frontend/src/hooks/useContexts.ts**
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { httpClient } from '../api/httpClient'
import type { Context } from './useContexts'

export const contextKeys = {
  all: ['contexts'] as const,
  lists: () => [...contextKeys.all, 'list'] as const,
  detail: (id: string) => [...contextKeys.all, 'detail', id] as const,
}

export function useContexts() {
  return useQuery({
    queryKey: contextKeys.lists(),
    queryFn: async () => {
      const response = await httpClient.get<Context[]>('/contexts')
      return response.data
    },
    staleTime: 1000 * 60 * 10, // 10 минут
  })
}

export function useCreateContext() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (data: Omit<Context, 'id' | 'user_id' | 'created_at'>) => {
      const response = await httpClient.post<Context>('/contexts', data)
      return response.data
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: contextKeys.lists() })
    },
  })
}
```

#### Файлы для изменения:
- `frontend/src/hooks/useContexts.ts`
- `frontend/src/hooks/useAreas.ts`
- `frontend/src/hooks/useProjects.ts`
- `frontend/src/hooks/useTags.ts`
- `frontend/src/hooks/useNotifications.ts`

---

### Шаг 3: IndexedDB кэш для данных (2-3 часа)

#### Задачи:
1. **Создание IndexedDB utility для кэша**
   - Файл: `frontend/src/lib/indexedDB.ts`
   - Функции: openDB, setCache, getCache, clearCache, deleteCache
   - Хранилище: `data-cache` с индексами по URL и timestamp

2. **Интеграция кэша в httpClient**
   - Файл: `frontend/src/api/httpClient.ts`
   - При GET запросе: проверка кэша → network → обновление кэша
   - При успешном GET: сохранение в кэш
   - При ошибке: возврат кэшированных данных

3. **Автоматическая очистка устаревшего кэша**
   - Очистка записей старше 24 часов
   - Лимит на размер кэша (100 записей)
   - Priority: недавние записи сохраняются

#### Реализация:

**frontend/src/lib/indexedDB.ts**
```typescript
const CACHE_DB_NAME = 'todowka-data-cache'
const CACHE_DB_VERSION = 1
const CACHE_STORE_NAME = 'cache'
const MAX_CACHE_SIZE = 100
const CACHE_MAX_AGE = 1000 * 60 * 60 * 24 // 24 часа

interface CacheEntry {
  url: string
  data: unknown
  timestamp: number
}

let db: IDBDatabase | null = null

export async function openCacheDB(): Promise<IDBDatabase> {
  if (db) return db

  return new Promise((resolve, reject) => {
    const request = indexedDB.open(CACHE_DB_NAME, CACHE_DB_VERSION)

    request.onerror = () => reject(request.error)
    request.onsuccess = () => {
      db = request.result
      resolve(db)
    }

    request.onupgradeneeded = (event) => {
      const database = (event.target as IDBOpenDBRequest).result
      if (!database.objectStoreNames.contains(CACHE_STORE_NAME)) {
        const store = database.createObjectStore(CACHE_STORE_NAME, { keyPath: 'url' })
        store.createIndex('timestamp', 'timestamp', { unique: false })
      }
    }
  })
}

export async function getCache<T>(url: string): Promise<T | null> {
  try {
    const database = await openCacheDB()
    return new Promise((resolve, reject) => {
      const transaction = database.transaction([CACHE_STORE_NAME], 'readonly')
      const store = transaction.objectStore(CACHE_STORE_NAME)
      const request = store.get(url)

      request.onsuccess = () => {
        const entry = request.result as CacheEntry | undefined
        if (!entry) {
          resolve(null)
          return
        }

        // Проверяем возраст кэша
        if (Date.now() - entry.timestamp > CACHE_MAX_AGE) {
          deleteCache(url).catch(console.error)
          resolve(null)
          return
        }

        resolve(entry.data as T)
      }
      request.onerror = () => reject(request.error)
    })
  } catch (error) {
    console.error('Failed to get cache:', error)
    return null
  }
}

export async function setCache<T>(url: string, data: T): Promise<void> {
  try {
    const database = await openCacheDB()
    return new Promise((resolve, reject) => {
      const transaction = database.transaction([CACHE_STORE_NAME], 'readwrite')
      const store = transaction.objectStore(CACHE_STORE_NAME)

      const entry: CacheEntry = {
        url,
        data,
        timestamp: Date.now(),
      }

      const request = store.put(entry)

      request.onsuccess = () => {
        // Проверяем размер кэша и удаляем старые записи
        cleanupCache(database).catch(console.error)
        resolve()
      }
      request.onerror = () => reject(request.error)
    })
  } catch (error) {
    console.error('Failed to set cache:', error)
  }
}

export async function deleteCache(url: string): Promise<void> {
  const database = await openCacheDB()
  return new Promise((resolve, reject) => {
    const transaction = database.transaction([CACHE_STORE_NAME], 'readwrite')
    const store = transaction.objectStore(CACHE_STORE_NAME)
    const request = store.delete(url)

    request.onsuccess = () => resolve()
    request.onerror = () => reject(request.error)
  })
}

export async function clearCache(): Promise<void> {
  const database = await openCacheDB()
  return new Promise((resolve, reject) => {
    const transaction = database.transaction([CACHE_STORE_NAME], 'readwrite')
    const store = transaction.objectStore(CACHE_STORE_NAME)
    const request = store.clear()

    request.onsuccess = () => resolve()
    request.onerror = () => reject(request.error)
  })
}

async function cleanupCache(database: IDBDatabase): Promise<void> {
  const entries = await new Promise<CacheEntry[]>((resolve, reject) => {
    const transaction = database.transaction([CACHE_STORE_NAME], 'readonly')
    const store = transaction.objectStore(CACHE_STORE_NAME)
    const request = store.getAll()

    request.onsuccess = () => resolve(request.result)
    request.onerror = () => reject(request.error)
  })

  // Сортируем по timestamp (новые сначала)
  const sorted = entries.sort((a, b) => b.timestamp - a.timestamp)

  // Удаляем записи старше CACHE_MAX_AGE и превышающие лимит
  const toDelete = sorted.slice(MAX_CACHE_SIZE).concat(
    sorted.filter(entry => Date.now() - entry.timestamp > CACHE_MAX_AGE)
  )

  const transaction = database.transaction([CACHE_STORE_NAME], 'readwrite')
  const store = transaction.objectStore(CACHE_STORE_NAME)

  for (const entry of toDelete) {
    store.delete(entry.url)
  }
}
```

**Интеграция в httpClient.ts:**
```typescript
import { getCache, setCache } from '../lib/indexedDB'

async function fetchWithAuth<T>(
  url: string,
  config: RequestConfig = {}
): Promise<ApiResponse<T>> {
  // ... существующий код ...

  try {
    // Проверяем кэш для GET запросов
    if (config.method === 'GET' || !config.method) {
      const cached = await getCache<T>(fullUrl)
      if (cached) {
        console.log('[Cache] Using cached data for:', fullUrl)
        // Возвращаем кэшированные данные, но фоново обновляем
        fetch(fullUrl, { ...fetchConfig, headers })
          .then(async (response) => {
            if (response.ok) {
              const data = await response.json()
              await setCache(fullUrl, data)
            }
          })
          .catch(console.error)
        return { data: cached, status: 200, statusText: 'OK' }
      }
    }

    const response = await fetch(fullUrl, { ...fetchConfig, headers })

    // ... существующая обработка ошибок ...

    const data = response.status === 204 ? null : await response.json()

    // Сохраняем в кэш для GET запросов
    if ((config.method === 'GET' || !config.method) && data) {
      await setCache(fullUrl, data)
    }

    return { data, status: response.status, statusText: response.statusText }
  } catch (error) {
    // При ошибке пробуем вернуть кэшированные данные
    if ((config.method === 'GET' || !config.method)) {
      const cached = await getCache<T>(fullUrl)
      if (cached) {
        console.log('[Cache] Using cached data after error for:', fullUrl)
        return { data: cached, status: 200, statusText: 'OK' }
      }
    }

    // ... существующая обработка ошибок ...
  }
}
```

#### Файлы для создания/изменения:
- `frontend/src/lib/indexedDB.ts` — новый файл
- `frontend/src/api/httpClient.ts` — интеграция кэша

---

### Шаг 4: Service Worker с Workbox (2-3 часа)

#### Задачи:
1. **Установка Workbox**
   ```bash
   npm install workbox-window workbox-precaching workbox-routing workbox-strategies workbox-expiration
   ```

2. **Настройка Workbox в Vite конфиге**
   - Файл: `frontend/vite.config.ts`
   - Плагин vite-plugin-pwa с workbox
   - Стратегии: NetworkFirst для API, CacheFirst для static

3. **Переписывание sw.ts с Workbox**
   - NetworkFirst стратегия для /api/*
   - CacheFirst стратегия для static assets
   - Экспирация кэша: 24 часа для API, 30 дней для static
   - Runtime caching для динамических ресурсов

4. **Обработка офлайн событий**
   - fallback страница для офлайн режима
   - Уведомления о статусе сети

#### Реализация:

**frontend/vite.config.ts**
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'mask-icon.svg'],
      manifest: {
        name: 'Todowka',
        short_name: 'Todowka',
        description: 'GTD Task Manager',
        theme_color: '#4f46e5',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
        runtimeCaching: [
          {
            urlPattern: /^\/api\/.*/,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'api-cache',
              expiration: {
                maxEntries: 100,
                maxAgeSeconds: 24 * 60 * 60, // 24 часа
              },
              cacheableResponse: {
                statuses: [0, 200],
              },
              networkTimeoutSeconds: 3,
            },
          },
          {
            urlPattern: /\.(?:png|jpg|jpeg|svg|gif|webp)$/,
            handler: 'CacheFirst',
            options: {
              cacheName: 'image-cache',
              expiration: {
                maxEntries: 60,
                maxAgeSeconds: 30 * 24 * 60 * 60, // 30 дней
              },
            },
          },
        ],
      },
    }),
  ],
})
```

**frontend/public/offline.html**
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Вы офлайн - Todowka</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      background: #f3f4f6;
    }
    .offline-container {
      text-align: center;
      padding: 2rem;
      max-width: 400px;
    }
    .offline-icon {
      font-size: 4rem;
      margin-bottom: 1rem;
    }
    h1 {
      color: #1f2937;
      margin-bottom: 0.5rem;
    }
    p {
      color: #6b7280;
      margin-bottom: 1.5rem;
    }
    button {
      background: #4f46e5;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 0.375rem;
      cursor: pointer;
      font-size: 1rem;
    }
    button:hover {
      background: #4338ca;
    }
  </style>
</head>
<body>
  <div class="offline-container">
    <div class="offline-icon">🔌</div>
    <h1>Вы офлайн</h1>
    <p>Проверьте подключение к интернету. Данные будут синхронизированы автоматически при восстановлении сети.</p>
    <button onclick="location.reload()">Обновить страницу</button>
  </div>
</body>
</html>
```

#### Файлы для создания/изменения:
- `frontend/vite.config.ts` — настройка Workbox
- `frontend/public/offline.html` — новая страница
- `frontend/src/sw.ts` — будет сгенерирован Workbox

---

### Шаг 5: Улучшенная обработка ошибок и retry (1-2 часа)

#### Задачи:
1. **Retry логика в httpClient**
   - Автоматический retry для 502, 503, 504
   - Exponential backoff: 1s, 2s, 4s, 8s, 16s, 32s (максимум 5 попыток)
   - Не retry для 4xx ошибок

2. **Индикация офлайн режима**
   - Компонент OfflineBanner для уведомления
   - Показ при network error или 502
   - Автоскрытие при восстановлении сети

3. **Оптимистичные обновления (опционально)**
   - Обновление UI до успешного ответа
   - Rollback при ошибке
   - Интеграция с React Query mutations

#### Реализация:

**frontend/src/components/OfflineBanner.tsx**
```typescript
import { useEffect, useState } from 'react'
import { useToastStore } from '../stores/toastStore'

export function OfflineBanner() {
  const [isOffline, setIsOffline] = useState(!navigator.onLine)

  useEffect(() => {
    const handleOnline = () => {
      setIsOffline(false)
      useToastStore.getState().addToast({
        title: 'Сеть восстановлена',
        body: 'Данные синхронизированы',
        type: 'success'
      })
    }

    const handleOffline = () => {
      setIsOffline(true)
      useToastStore.getState().addToast({
        title: 'Вы офлайн',
        body: 'Работаем с кэшированными данными',
        type: 'error'
      })
    }

    window.addEventListener('online', handleOnline)
    window.addEventListener('offline', handleOffline)

    return () => {
      window.removeEventListener('online', handleOnline)
      window.removeEventListener('offline', handleOffline)
    }
  }, [])

  if (!isOffline) return null

  return (
    <div className="fixed top-0 left-0 right-0 bg-amber-500 text-white py-2 px-4 text-center z-50">
      <div className="flex items-center justify-center gap-2">
        <span className="text-xl">🔌</span>
        <span className="font-medium">Вы офлайн — работаем с кэшированными данными</span>
      </div>
    </div>
  )
}
```

**Retry логика в httpClient.ts:**
```typescript
async function fetchWithRetry<T>(
  fn: () => Promise<ApiResponse<T>>,
  maxRetries = 3
): Promise<ApiResponse<T>> {
  let lastError: Error | null = null

  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn()
    } catch (error) {
      lastError = error as Error

      // Не retry для 4xx ошибок
      if (error instanceof ApiError && error.status >= 400 && error.status < 500) {
        throw error
      }

      // Retry для 5xx ошибок и network errors
      if (attempt < maxRetries) {
        const delay = Math.min(1000 * 2 ** attempt, 30000)
        console.log(`[Retry] Attempt ${attempt + 1}/${maxRetries} after ${delay}ms`)
        await new Promise(resolve => setTimeout(resolve, delay))
      }
    }
  }

  throw lastError
}
```

#### Файлы для создания/изменения:
- `frontend/src/components/OfflineBanner.tsx` — новый компонент
- `frontend/src/api/httpClient.ts` — retry логика
- `frontend/src/main.tsx` — интеграция OfflineBanner

---

### Шаг 6: Тестирование и оптимизация (2-3 часа)

#### Задачи:
1. **Тестирование офлайн режима**
   - Отключить бэкенд
   - Проверить загрузку приложения
   - Проверить отображение кэшированных данных
   - Проверить создание/редактирование/удаление задач офлайн
   - Включить бэкенд
   - Проверить автоматическую синхронизацию

2. **Тестирование кэша**
   - Проверить сохранение данных в IndexedDB
   - Проверить очистку устаревшего кэша
   - Проверка лимита размера кэша

3. **Оптимизация производительности**
   - Проверить размер IndexedDB
   - Оптимизировать размер кэшируемых данных
   - Удалить ненужные данные из кэша

4. **Edge cases**
   - Частое включение/выключение сети
   - Проверка при медленном соединении
   - Проверка при частичных сбоях (некоторые API работают, другие нет)

---

## Порядок реализации

### День 1: Базовая инфраструктура
1. **Утро (2-3 часа)**: Шаг 1 — Установка и настройка React Query
2. **День (3-4 часа)**: Шаг 2 — Миграция хуков на React Query

### День 2: Кэширование и Service Worker
1. **Утро (2-3 часа)**: Шаг 3 — IndexedDB кэш для данных
2. **День (2-3 часа)**: Шаг 4 — Service Worker с Workbox

### День 3: Полировка и тестирование
1. **Утро (1-2 часа)**: Шаг 5 — Улучшенная обработка ошибок
2. **День (2-3 часа)**: Шаг 6 — Тестирование и оптимизация

---

## Проверочный список завершения этапа 8

### Функциональность
- [x] React Query установлен и настроен
- [x] Все хуки мигрированы на React Query (useTasks, useContexts, useAreas, useProjects, useTags) - useNotifications использует Zustand стор
- [ ] IndexedDB кэш работает для GET запросов
- [ ] Данные загружаются из кэша при ошибках сети
- [ ] Service Worker с Workbox генерируется и работает
- [ ] NetworkFirst стратегия работает для API запросов
- [ ] Офлайн режим работает (показываются кэшированные данные)
- [ ] Автоматическая синхронизация при восстановлении сети
- [ ] OfflineBanner показывает статус сети
- [ ] Toast уведомления о статусе сети и синхронизации

### Техническое качество
- [ ] Линтинг `npm run lint` без ошибок
- [ ] Type checking `npx tsc --noEmit` без ошибок
- [ ] Нет утечек памяти
- [ ] Обработка всех edge cases
- [ ] Адекватный размер IndexedDB (< 50MB)
- [ ] Производительность не ухудшилась

### Пользовательский опыт
- [ ] Понятные уведомления о статусе сети
- [ ] Нет визуальных артефактов при загрузке кэшированных данных
- [ ] Плавные анимации и переходы
- [ ] Интуитивно понятный интерфейс

---

## Оценка времени

| Шаг | Оценка времени | Фактическое время |
|-----|----------------|-------------------|
| Шаг 1: React Query | 2-3 часа | ~30 мин |
| Шаг 2: Миграция хуков | 3-4 часа | ~15 мин |
| Шаг 3: IndexedDB кэш | 2-3 часа | ___ |
| Шаг 4: Service Worker | 2-3 часа | ___ |
| Шаг 5: Обработка ошибок | 1-2 часа | ___ |
| Шаг 6: Тестирование | 2-3 часа | ___ |
| **Итого** | **12-18 часов** | **~45 мин** |

---

## Известные ограничения

### Текущие ограничения (Вариант 6):
1. **Нет conflict resolution** — при одновременном редактировании используется last write wins
2. **Нет partial updates** — при ошибке синхронизации вся мутация не выполняется
3. **Оптимистичные обновления** — опционально, требует дополнительного времени

### Future improvements:
1. **Conflict resolution** — ручное разрешение коллизий
2. **Partial sync** — синхронизация только успешных мутаций
3. **Diff sync** — отправка только изменений (delta)
4. **Compression** — сжатие данных в IndexedDB
5. **Background sync via Service Worker** — расширенная поддержка

---

## Ссылки

- [React Query документация](https://tanstack.com/query/latest/docs/react/overview)
- [Workbox документация](https://developer.chrome.com/docs/workbox/)
- [PWA Best Practices](https://web.dev/progressive-web-apps/)
- [IndexedDB API](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API)
- [Основной план итерации 3](./план.md)
- [Этап 7: Offline синхронизация](./этап-7-offline-синхронизация.md)
- [Features documentation](../../features.md)
