# Анализ и план исправления оффлайн-функциональности

**Дата:** 2026-04-16
**Статус:** Требует реализации

---

## 1. Текущее состояние

Инфраструктура оффлайн частично реализована:

- PWA + Service Worker с прекэшем и runtime-caching
- IndexedDB-кэш HTTP-ответов
- Персистентность React Query через IndexedDB
- Офлайн-очередь мутаций (useOfflineQueue)
- Локальные изменения задач (localTaskChanges)
- Optimistic updates для updateTask
- OfflineBanner + тосты

**Но всё это не работает из-за критических багов.**

---

## 2. Найденные баги

### КРИТИЧЕСКИЕ (блокируют работу)

#### БАГ-1: `queueMutationFn` = `null` при старте приложения

**Файл:** `httpClient.ts:45`, `useOfflineQueue.ts`

`useOfflineQueue` монтируется только внутри `TaskEditModal`. До первого открытия модалки глобальная функция очереди = `null`. Все мутации (создание, toggle, move, delete) молча теряются в офлайне — `httpClient.ts:215` проверяет `if (queueMutationFn && ...)` и получает `false`.

**Исправление:** Примонтировать `useOfflineQueue` на уровне корневого компонента (App/layout), чтобы `queueMutationFn` инициализировался при старте приложения.

---

#### БАГ-2: `addTask` — нет offline-обработки

**Файл:** `useTasks.ts:206-215, 337-346`

Мутация `addTaskMutation` не имеет:
- `onMutate` — нет optimistic update, задача не появляется в UI
- `onError` с проверкой `OfflineQueueError` — ошибка просачивается как generic

Обёртка `addTask`:
```typescript
catch (err) {
  if (err instanceof ApiError) { throw err }
  throw new Error('Failed to add task')  // OfflineQueueError попадает сюда
}
```

`OfflineQueueError extends Error`, не `ApiError`. Пользователь видит "Failed to add task". Задача не появляется в UI.

**Исправление:**
1. Добавить `onMutate` с optimistic update (создать временную задачу с локальным ID)
2. Добавить `onError` — при `OfflineQueueError` не откатывать изменения
3. В обёртке `addTask` — подавлять `OfflineQueueError` и возвращать локальную задачу

---

#### БАГ-3: `deleteTask` / `toggleTask` / `moveTask` — та же проблема

**Файл:** `useTasks.ts:302-335, 359-390`

- Нет `onMutate` — визуально ничего не меняется
- Нет `onError` для `OfflineQueueError`
- Обёртки ловят `ApiError`, но `OfflineQueueError` просачивается как generic error

**Исправление:** Добавить optimistic updates + `onError` для `OfflineQueueError` во все мутации.

---

#### БАГ-4: `getAllLocalTaskChanges` не экспортируется

**Файл:** `useLocalTaskChanges.ts:6`, `localTaskChanges.ts`

Импорт функции, которой нет в модуле. Runtime-ошибка при вызове `getAllChangedTaskIds()`.

**Исправление:** Добавить экспорт `getAllLocalTaskChanges` в `localTaskChanges.ts`:
```typescript
export async function getAllLocalTaskChanges(): Promise<Record<string, LocalTaskChange>> {
  const db = await openDB('todowka-local-changes', 1, ...)
  return await db.getAll('task-changes')
}
```

---

#### БАГ-5: HTTP 500+ = "офлайн" (дублирование данных)

**Файл:** `httpClient.ts:141`

```typescript
const isNetworkError = response.status >= 500 || response.status === 0
```

`status >= 500` — сервер ответил (сеть работает), но мутация ставится в очередь. При "восстановлении" сети мутация повторится → дублирование.

**Исправление:** Убрать `status >= 500` из условия. Ставить в очередь только при `TypeError` от fetch (нет сети) и `status === 0`.

---

#### БАГ-6: `ONLINE_RECONNECT` — никто не слушает

**Файл:** `httpClient.ts:298`

Событие отправляется, но нигде нет `addEventListener('ONLINE_RECONNECT', ...)`. Синхронизация при восстановлении сети может не запуститься.

**Исправление:** Добавить слушатель `ONLINE_RECONNECT` в `useOfflineQueue`:
```typescript
useEffect(() => {
  const handleReconnect = () => { syncQueue() }
  window.addEventListener('ONLINE_RECONNECT', handleReconnect)
  return () => window.removeEventListener('ONLINE_RECONNECT', handleReconnect)
}, [])
```

---

### СЕРЬЁЗНЫЕ (нарушают UX)

#### БАГ-7: Areas / Projects / Contexts / Tags — без offline

**Файлы:** `useAreas.ts`, `useProjects.ts`, `useContexts.ts`, `useTags.ts`

Мутации этих сущностей не обрабатываются офлайн. Нет optimistic updates, нет `OfflineQueueError` обработки. При офлайне пользователь видит generic ошибку, изменение теряется.

**Исправление:** Добавить optimistic updates + `OfflineQueueError` обработку по аналогии с задачами.

---

#### БАГ-8: `SyncIndicator` нигде не смонтирован

**Файл:** `SyncIndicator.tsx`

Компонент готов, но не подключён. Пользователь не видит:
- Сколько мутаций в очереди
- Идёт ли синхронизация
- Что данные не синхронизированы

**Исправление:** Примонтировать `SyncIndicator` в layout (например, в Header или Sidebar).

---

#### БАГ-9: `clearAllLocalTaskChanges` после sync — потеря новых изменений

**Файл:** `useOfflineQueue.ts:213-216`

Если пользователь внёс изменения во время синхронизации, новые локальные изменения будут удалены.

**Исправление:** Заменить `clearAllLocalTaskChanges()` на granular-очистку — удалять только те `localTaskChanges`, чьи мутации успешно синхронизированы:
```typescript
for (const mutation of syncedMutations) {
  if (mutation.url includes taskId) {
    await deleteLocalTaskChange(taskId)
  }
}
```

---

#### БАГ-10: Дублирование toast-уведомлений

**Файлы:** `httpClient.ts:284-313`, `useOfflineQueue.ts:101-112`

Оба модуля независимо слушают `online`/`offline`. Пользователь видит дублированные уведомления.

**Исправление:** Оставить тосты только в одном месте — в `useOfflineQueue`, убрать из `httpClient`.

---

## 3. План исправления

### Этап 1: Критические исправления ядра (P0)

**Задача 1.1: Примонтировать `useOfflineQueue` глобально**
- Файлы: корневой layout / App компонент
- Убрать `useOfflineQueue` из `TaskEditModal`
- Примонтировать на уровне App, чтобы `queueMutationFn` инициализировался при старте
- **Проверка:** После загрузки приложения `queueMutationFn !== null`

**Задача 1.2: Исправить `httpClient.ts` — убрать 500+ из условия офлайна**
- Файл: `httpClient.ts:141`
- Заменить `response.status >= 500 || response.status === 0` на `response.status === 0`
- 500+ ошибки должны обрабатываться как обычные ошибки сервера, не ставить в очередь
- **Проверка:** При 500 ошибке мутация НЕ ставится в очередь

**Задача 1.3: Добавить экспорт `getAllLocalTaskChanges`**
- Файл: `lib/localTaskChanges.ts`
- Добавить функцию `getAllLocalTaskChanges()` которая возвращает все записи из IndexedDB
- **Проверка:** `tsc --noEmit` без ошибок

**Задача 1.4: Подключить слушатель `ONLINE_RECONNECT`**
- Файл: `hooks/useOfflineQueue.ts`
- Добавить `addEventListener('ONLINE_RECONNECT', ...)` → вызов `syncQueue()`
- **Проверка:** При восстановлении сети sync запускается корректно

**Задача 1.5: Исправить optimistic updates для задач**
- Файл: `hooks/useTasks.ts`
- Добавить `onMutate` для `addTask`: создать временную задачу с локальным UUID
- Добавить `onMutate` для `deleteTask`: убрать из кэша
- Добавить `onMutate` для `toggleTask`: переключить completed
- Добавить `onMutate` для `moveTask`: обновить area_id
- Добавить `onError` для всех мутаций: при `OfflineQueueError` — не откатывать
- В обёртках: подавлять `OfflineQueueError`, возвращать локальный результат
- **Проверка:** При офлайне изменения видны в UI, нет ошибок

### Этап 2: UX и синхронизация (P1)

**Задача 2.1: Примонтировать `SyncIndicator`**
- Добавить в layout (Header/Sidebar)
- Показывать: online/offline, размер очереди, статус синхронизации
- **Проверка:** Индикатор виден в UI

**Задача 2.2: Granular-очистка localTaskChanges**
- Файл: `hooks/useOfflineQueue.ts`
- Заменить `clearAllLocalTaskChanges()` на удаление только синхронизированных
- **Проверка:** Новые локальные изменения не теряются при sync

**Задача 2.3: Offline для Areas / Projects / Contexts / Tags**
- Файлы: `useAreas.ts`, `useProjects.ts`, `useContexts.ts`, `useTags.ts`
- Добавить optimistic updates для всех мутаций
- Добавить `onError` для `OfflineQueueError`
- Подавлять ошибки в обёртках
- **Проверка:** CRUD этих сущностей работает офлайн

**Задача 2.4: Убрать дублирование тостов**
- Убрать тосты из `httpClient.ts`
- Оставить только в `useOfflineQueue.ts`
- **Проверка:** По одному тосту при online/offline

### Этап 3: Улучшения (P2)

**Задача 3.1: Delta-синхронизация на бэкенде**
- Добавить параметр `updated_since` в `GET /api/tasks`
- Добавить поле `updated_at` в ответах всех сущностей
- Фронтенд при reconect запрашивает только изменения
- **Проверка:** При reconect загружаются только изменившиеся данные

**Задача 3.2: Версионность задач**
- Добавить поле `version` (integer) в модель Task
- Реализовать optimistic concurrency control
- При конфликте — показывать диалог разрешения
- **Проверка:** Конфликт обнаруживается и разрешается

**Задача 3.3: Background Sync API**
- Зарегистрировать `ServiceWorkerRegistration.sync`
- Гарантированная отправка мутаций при восстановлении сети
- **Проверка:** Мутации отправляются даже при закрытом приложении

---

## 4. Порядок реализации

```
Этап 1 (P0) — можно делать параллельно:
  1.1 → глобальный монтаж useOfflineQueue
  1.2 → исправить httpClient
  1.3 → экспорт getAllLocalTaskChanges
  1.4 → слушатель ONLINE_RECONNECT
  1.5 → optimistic updates для задач (зависит от 1.1)

Этап 2 (P1) — после Этапа 1:
  2.1 → SyncIndicator
  2.2 → granular-очистка
  2.3 → offline для Areas/Projects/Contexts/Tags
  2.4 → дублирование тостов

Этап 3 (P2) — после Этапа 2:
  3.1 → delta-синхронизация
  3.2 → версионность
  3.3 → Background Sync API
```

## 5. Оценка времени

| Этап | Время | Сложность |
|------|-------|-----------|
| Этап 1 | 4-6 часов | Средняя |
| Этап 2 | 4-6 часов | Средняя |
| Этап 3 | 6-8 часов | Высокая |
| **Итого** | **14-20 часов** | |

## 6. Риски

1. **IndexedDB может быть очищен браузером** при нехватке памяти — нужна обработка
2. **Офлайн-создание задач** требует генерации локального UUID и маппинга на серверный ID после sync
3. **Множественные мутации одной задачи** — нужно схлопывать промежуточные состояния
4. **Параллельное редактирование** на разных устройствах — без версионности возможна потеря данных
