# API base URL for backend requests (e.g., /api for production or http://localhost:8000/api for development)
VITE_API_BASE_URL=/api

# Application name displayed in UI
VITE_APP_NAME=Todowka

# Note: Variables must start with VITE_ to be available in the frontend code
